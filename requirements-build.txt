# Requirements for building executables
# Install with: pip install -r requirements-build.txt

pyinstaller>=6.0.0

# Core dependencies (for bundling)
coincurve>=19.0.0
cryptography>=42.0.0
pydantic>=2.5.0
asyncio-pool>=0.6.0
plyvel>=1.5.0
fastapi>=0.109.0
uvicorn>=0.27.0
pyzmq>=25.1.0
aiohttp>=3.9.0
